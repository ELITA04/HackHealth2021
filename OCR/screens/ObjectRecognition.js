import { StyleSheet, Text, View, Button } from 'react-native';
import React from 'react'

const ObjectRecognitionScreen = ({ navigation, route }) => {
    return <Text>OCR Screen</Text>;
  };

export default ObjectRecognitionScreen;